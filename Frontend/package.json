{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html script.js style.css login.html login.js config.js assets dev-icons dist/ || (mkdir dist && xcopy /E /I . dist)"
  },
  "dependencies": {
    "jsdom": "^29.0.1"
  }
}
